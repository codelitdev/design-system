# Dependencies
node_modules/

# Windows/WSL Alternate Data Stream metadata (created when copying files in)
*:Zone.Identifier

# npm auth — never commit (holds registry tokens)
.npmrc

# design-sync build output + staged scripts (regenerated per run)
ds-bundle/
.ds-sync/

# design-sync machine state (not durable)
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
