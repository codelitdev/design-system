Native select styled like Input, with a chevron affordance.

```jsx
<Select label="Audience" options={["Everyone", "Enrolled students", "Team only"]} />
```

Options accept strings or `{value, label}`.
