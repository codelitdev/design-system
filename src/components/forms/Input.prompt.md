Text field with label/hint/error built in; `multiline` turns it into a textarea.

```jsx
<Input label="Course title" placeholder="e.g. Intro to TypeScript" hint="Shown on your public page." />
<Input label="From address" error="That doesn't look like an email." />
```

Focus shows an accent-wash ring; errors switch to destructive.
