Small dark tooltip above the wrapped element on hover; keep labels to a few words.

```jsx
<Tooltip label="Copy upload URL"><IconButton label="Copy"><CopyIcon size={16} /></IconButton></Tooltip>
```

`open` forces it visible for specimens.
