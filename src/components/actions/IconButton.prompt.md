Icon-only square button for toolbars and table-row actions; pass a Lucide icon as child and always set `label`.

```jsx
<IconButton label="Copy URL"><CopyIcon size={16} /></IconButton>
```

Variants: ghost (default), outline. Sizes: md 34px, sm 28px.
